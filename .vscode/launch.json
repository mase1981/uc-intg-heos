{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: heos Integration",
            "type": "debugpy",
            "request": "launch",
            "module": "uc_intg_heos.driver",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "justMyCode": false,
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}